name: sjmcl
base: core22
version: '0.0.0'
summary: SJMCL Minecraft Launcher
description: |
  SJMC Minecraft Launcher. A modern, lightweight Minecraft launcher.

grade: stable
confinement: strict

apps:
  sjmcl:
    command: usr/bin/SJMCL
    desktop: usr/share/applications/SJMCL.desktop
    extensions: [gnome]
    environment:
      GTK_USE_PORTAL: "1"
    plugs:
      - desktop
      - desktop-legacy
      - gsettings
      - wayland
      - x11
      - network
      - network-bind
      - opengl
      - audio-playback
      - removable-media
      - home

parts:
  sjmcl-payload:
    plugin: dump
    source: snap-payload
    stage-packages:
      - libwebkit2gtk-4.1-0
      - libayatana-appindicator3-1
      - libcanberra-gtk3-module

  cleanup:
    after:
      - sjmcl-payload
    plugin: nil
    build-snaps:
      - core22
      - gnome-42-2204
    override-prime: |
      set -eux
      for snap in "core22" "gnome-42-2204"; do
        cd "/snap/$snap/current"
        find . -type f,l -exec rm -f "$CRAFT_PRIME/{}" \;
      done